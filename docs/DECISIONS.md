# Decisions

**Status:** Accepted choices log for Number Go Up.
**Use:** Grep by ID (for example `grep -n "^## D004" docs/DECISIONS.md`); never read the whole file during routine work.

Rules:

- IDs are permanent and never reused once cited.
- Entries are appended in acceptance order and dated.
- A superseded entry stays in place, names its replacement, and keeps its history.
- Only accepted choices belong here. A recommendation in conversation is not a decision until the owner accepts it.

---

## D001 — Replace percentage tax with two absolute axes

- **Status:** Accepted (2026-09-21). The one-for-one production rule and its "production is both offence and survival" consequence are superseded by D012; the two absolute axes stand.
- **Context:** The prototype deducted a percentage of current Number per wave, so weak and strong builds lost the same fraction and production growth did not extend survival. The Tower's structure separates an enemy health pool from an enemy attack.
- **Decision:** A Tax encounter has absolute Liability (depleted one-for-one by Number production) and absolute Collection (deducted from Number at each wave boundary while Liability remains).
- **Consequences:** Production is both offence and survival; balance lives in `TaxBalanceProfile`; Number reaching zero ends the run.
- **Revisit when:** Playtest shows a single axis dominates, or a third axis is proposed (for example mitigation separate from Number).

## D002 — Explicit tiers with smaller reward steps than pressure steps

- **Status:** Accepted (2026-09-21)
- **Context:** Tiered difficulty needs an honest jump without making the hardest tier automatically the best farm.
- **Decision:** Author Tiers 1–3 only. Tier 2 and 3 apply exactly 20× and 60× Tier 1 Liability and Collection from wave 1, with rewards of 1.8× and 2.6×. Tiers unlock by clearing wave 100 of the preceding tier.
- **Consequences:** Higher tiers are a difficulty choice first; reward ratios stay honest at equal pressured waves; later rows stay out of the save and UI until authored.
- **Revisit when:** The three-tier horizon is cleared, or playtest shows a tier is never worth entering.

## D003 — Retreat ends a run; active runs freeze offline

- **Status:** Accepted (2026-09-21)
- **Context:** The prototype let closure stop tax while Number kept growing, creating a pause-and-grow exploit.
- **Decision:** Retreat ends and resets the run, banking earned Coins. An active run freezes exactly while the app is away, and offline time is never injected into a run.
- **Consequences:** No pause state exists; Number production happens only during active play; death and retreat can share reset machinery.
- **Revisit when:** A future design requires genuine mid-run saving and can prove it cannot become a growth exploit.

## D004 — Workshop is permanent; Number is run-only

- **Status:** Accepted (2026-09-21)
- **Context:** A run-scoped Workshop reset every attempt, which made permanent progress only Knowledge/Insight and left Coins with little long-term purpose. The Tower keeps a permanent Workshop baseline separate from in-run cash upgrades.
- **Decision:** The four Workshop bays are bought with Coins between runs and permanently raise every later run's baseline. Number and lifetime production exist only during an active run and reset on every ending.
- **Consequences:** Coin rewards fund permanent power; Workshop purchases are locked during runs; a fresh run starts from `starting_number_flat` plus permanent effects, never from banked Number; a future temporary in-run upgrade layer must have its own name and resource.
- **Revisit when:** Between-run choices stop feeling meaningful, or an in-run layer is designed and needs the boundary restated.

## D005 — One ordered modifier pipeline for all future rules

- **Status:** Accepted (2026-09-21)
- **Context:** Laws, Violations, perks, challenges and tier conditions will each want to adjust encounter numbers, and per-system special cases inside `GameState` would make stacking unpredictable.
- **Decision:** All rule modifications pass through `RuleModifierPipeline` with stages applied in order: flat → additive → multiplicative → cap_max → cap_min, each applied once.
- **Consequences:** Stacking is documented and testable; new systems provide modifiers, not branches in state code.
- **Revisit when:** A rule needs an effect the five stages cannot express; extend the pipeline stage list with a decision rather than bypassing it.

## D006 — Deterministic run seeds and saved RNG state

- **Status:** Accepted (2026-09-21)
- **Context:** Randomised-only RNG made balance failures hard to reproduce and saves non-deterministic.
- **Decision:** Every run stores a seed; the save stores the RNG state; a loaded active encounter resumes with identical remaining Liability and identical random outcomes.
- **Consequences:** Runs are reproducible for testing and debugging; balance simulation uses fixed seeds.
- **Revisit when:** Run-scoped random content (offers, drops) is added and needs its own stream separation.

## D007 — Save schema V4 with explicit migrations

- **Status:** Accepted (2026-09-21). Its writer has since moved to V5 ([D017](#d017--save-schema-v5-for-the-four-workshop-categories)) and then V6 ([D028](#d028--save-schema-v6-and-never-writing-over-a-save-the-loader-cannot-read)); the migration rule stands.
- **Context:** The permanent Workshop and run-only Number changed what a save must contain.
- **Decision:** Save schema V4 stores the permanent Workshop, active encounter and RNG state. V1/V2/V3 saves migrate forward without losing declared permanent progress. Banked pre-V4 Number retires because Number is run-only.
- **Consequences:** `SaveDataV4` owns the shape; older schemas stay readable for migration only; migration writes the upgraded save immediately.
- **Revisit when:** A new permanent system adds saved state; bump the schema and extend migration rather than widening V4 silently.

## D008 — What survives a reset

- **Status:** Accepted (2026-09-21)
- **Context:** The reset boundary needs to be unambiguous for players and for future systems.
- **Decision:** Workshop ranks, Coins, Knowledge, Insight ranks, Shield Matrix rank, tier records and highest Number survive every run ending. Number, lifetime production, momentum, critical chain and the active encounter reset. Research Focus survives death and retreat and clears on Prestige.
- **Consequences:** "Start over, a bit stronger" is the loop; no future layer may blur which side of the line it lives on.
- **Revisit when:** A new permanent or temporary layer is proposed; state its side of the boundary explicitly.

## D009 — The Tower is a shape reference, not a content source

- **Status:** Accepted (2026-09-21)
- **Context:** The research in `TOWER_SCALING_FOUNDATION.md` drew on public SDK data, wikis and reference calculators.
- **Decision:** Borrow the architecture (two axes, explicit tiers, milestone spine, smaller reward steps), but author Number Go Up's own coefficients, names, pacing and content. Exact reference constants are evidence, not transplants.
- **Consequences:** No copied tables, names, art or extracted data; coefficients stay inspectable in `TaxBalanceProfile`.
- **Revisit when:** A new reference game is studied; apply the same clean-room boundary.

## D010 — Grace waves pay repeatable Coins

- **Status:** Accepted (2026-09-21). The grace itself was replaced by [D033](#d033--tier-1-opens-under-attack)'s warm-up, and that by [D040](#d040--one-difficulty-curve-for-tier-1-from-wave-1)'s single curve (2026-09-23): every wave now pays 0.65 × its number in Coins from wave 1. The first failed run still funds permanent Workshop progress, now about 100 Coins rather than 48.
- **Context:** With a permanent Coin-funded Workshop, a first run that earned nothing until wave 21 would leave a new player with no visible progress.
- **Decision:** Tier 1 grace waves pay 1 Coin per wave and 5 on boss waves, plus the existing milestone bonus at wave 10.
- **Consequences:** A representative first failure banks 48 Coins, enough for the first two Workshop ranks; grace is onboarding, not empty time.
- **Revisit when:** First-run pacing is retuned, or grace rewards are found to trivialise early permanent progression.

## D011 — Product direction: simple surface, deep systems

- **Status:** Accepted (2026-09-21)
- **Context:** The owner wants a very simple game with incredibly deep systems to carry it forward, using The Tower and tech-tree games as the main inspirations.
- **Decision:** [`GAME_VISION.md`](GAME_VISION.md) owns the product ambition, pillars and anti-goals. New systems are admitted by the vision's tests and must keep the run loop the game rather than skipping it.
- **Consequences:** Feature proposals are judged against the vision before implementation; currency count stays minimal; no live-operations scaffolding before the core run is proven fun.
- **Revisit when:** The vision's pillars or reference set change; update the vision document first, then this entry.

## D012 — Output beats the wave before it becomes Number

- **Status:** Superseded by [D037](#d037--the-number-always-rises-missed-waves-move-on-bosses-stay-and-fight) (2026-09-23): the Number rises from all output again. Accepted (2026-09-21). Supersedes D001's one-for-one production rule. Implemented (2026-09-21) with balance profile `tax-foundation-v2`: Liability and Collection halved, pressured wave Coins ×0.65. A wave that outlasts its timer keeps its HP everywhere but Tier 1's warm-up, where it ends on its timer ([D033](#d033--tier-1-opens-under-attack)).
- **Context:** Every unit produced raised Number and also depleted Liability, so production healed the player while stuck on a wave. Attack did Defense's job: a max-Attack Tier 1 run survived 102 Collection hits over 46 minutes. Pillar 2 had nothing to stand on, and the Number rose whether the player was winning or losing.
- **Decision:** Output applies to the active wave's remaining Liability first; only output beyond it is added to Number. A wave that outlasts its timer still deals its Collection and keeps its remaining Liability, as today. `lifetime_generated` keeps counting all output.
- **Consequences:** Collection hits become real events, Defense gets a job Attack cannot do, and the Number rises only while the player is ahead. Measured in [`WORKSHOP_DESIGN.md`](WORKSHOP_DESIGN.md): on the v1 curves the first run was unchanged (wave 21, 48 Coins), but Tier 1 wave 100 moved out of reach at the same investment and Tier 2 turned harsh for builds below the unlock level. D012 therefore shipped together with the v2 retune, which restores wave 100 for the same build in 45 minutes rather than 73, with 82 hits rather than 194. On its own it does not make Defense an equal axis; that needs new Defense stats and tuning where the hit decides the fight.
- **Revisit when:** The retune cannot meet the first-run and Coin-rate targets together, or playtest shows a Number sitting flat through a wave reads as failure rather than pressure.

## D013 — The Workshop has four categories: Attack, Defense, Utility, Ultimates

- **Status:** Accepted (2026-09-21). Implemented (2026-09-21): the four categories, the Armor row and the category-shaped Research Focus ship with save schema V5 (D017).
- **Context:** All twelve Workshop upgrades served production; Defense existed only as Shield Matrix and Brace, outside the Workshop. With two checks per wave, each needs a shelf, and the owner wants the Workshop to be a large part of the game, as it is in The Tower.
- **Decision:** The Workshop is organised as Attack (beat the wave inside its timer), Defense (survive the hits when you can't), Utility (get more from every run) and Ultimates (milestone-unlocked abilities that fire on their own). The Output, Speed, Chance and Logic bays retire into these categories and Shield Matrix becomes the Defense stat Armor. Every stat carries a one-line player-facing reason to buy it. The stat list, presentation and strategy set live in [`WORKSHOP_DESIGN.md`](WORKSHOP_DESIGN.md).
- **Consequences:** Retiring bays is a save schema V5 migration covering `selected_bay`, `focus` (Research Focus discounts by bay today), the bay unlock gates and `tax_resistance_rank`, with no loss of ranks; upgrade ids stay stable. Research Focus retargets from a bay to a category. Vision pillar 8 changes from four bays to four categories.
- **Revisit when:** A category has no stat worth buying at some stage of the game, or a fifth shelf is proposed.

## D014 — Player-facing vocabulary: Wave HP, Hit, Armor

- **Status:** Accepted (2026-09-21). Implemented (2026-09-22) across the run screen, hit feedback, tap floats and the stats drawer.
- **Context:** Tax phrasing (Liability, Collection, Compliance, Shield Matrix) made the two checks harder to read than they need to be. The owner wanted plain words that make every Workshop stat's purpose obvious.
- **Decision:** Player-facing text uses Wave, Wave HP (shown as the ring), Damage, Beaten, Hit, Warm-up wave, Brace and Armor, mapped term by term in [`WORKSHOP_DESIGN.md`](WORKSHOP_DESIGN.md). The Number is never called health on screen. Code names and save keys keep their current names.
- **Consequences:** The UI string pass replaced the tax wording. Renaming classes is a separate mechanical change, and renaming a save key would need a migration; neither is implied.
- **Revisit when:** Playtest shows a term being misread, or a new mechanic needs a word the set lacks.

## D015 — The Rig: the same four categories inside a run, bought with Number

- **Status:** Its rule that prices are quoted against the wave's HP is superseded by [D039](#d039--rig-prices-follow-the-players-income-not-the-wave) (2026-09-23). Accepted (2026-09-21) for the shape the owner directed — Attack, Defense, Utility and Ultimate on the run screen, as The Tower places them — and for Number as the resource, confirmed by the owner the same day. The domain core and its balance targets are implemented (2026-09-22, D023): run-scoped ranks in `GameState`, prices in `TaxBalanceProfile`, a 3× effect multiplier, combined defensive ceilings, saved with the active run and cleared by every ending. Targets 7–9 pass; the panel shipped (2026-09-22) and sits below the Number ([D032](#d032--the-run-screen-keeps-the-number-on-top-and-the-rig-below-it)), and early and mid builds not profiting from the Rig was the open playtest question (after D039, mid builds reach the same wave faster and with more Coins when they spend; early builds still reach a wave or so less) (see [`WORKSHOP_DESIGN.md`](WORKSHOP_DESIGN.md)).
- **Context:** A run contains one decision today — Brace, at a flat 30% — and tapping. D012 made the Number an honest scoreboard that rises only while the player is ahead, but gave them nothing to do with being ahead. The vision has held an open question since D004: when a temporary in-run upgrade layer arrives, what is it called and what does it spend?
- **Decision:** The Rig is a run-scoped upgrade layer with the same four categories and the same stat catalogue as the Workshop, bought with Number during a run and lost when the run ends. Prices are quoted against the current wave's HP rather than in absolute Number, so they scale across tiers and depth without per-tier data. Rig ranks are uncapped and limited by cost growth alone. Brace becomes the Defense tab's first row; Shield leaves the run screen and becomes Armor in the Workshop under D013. The Rig grants no Coins, no Knowledge and no record, and opens no new RNG stream.
- **Consequences:** Pillar 3's "distinct name and resource" is satisfied by Number, which is already the run-only resource, so no fourth currency is added (pillar 4). Attack and Defense compete for one pool inside the run, which is pillar 2 as a moment-to-moment choice. Rig ranks are run state: saved with the active run under D006 and D007, cleared with every ending under D008, and part of the save schema V5 bump that D013's category migration already requires. A player can spend themselves to death; the panel warns against the incoming hit and sells anyway, because the contract is that the number is visible before it kills you, not that the game refuses the decision. Free Upgrade is "every Nth purchase" rather than a roll, so D006's seed reproducibility is untouched.
- **Revisit when:** The balance targets for the Rig cannot be met together — in particular if the Rig lets a fresh permanent build reach waves that previously needed Workshop investment, which would make the meta loop optional.

## D016 — The bottom bar carries what is actionable now

- **Status:** Accepted (2026-09-21). The Workshop half is implemented (2026-09-21) and the between-runs bar, the Knowledge sheet and the run-over doors in step 5 (2026-09-22); the in-run half waits on the Rig (D015). Refined the same day by [D018](#d018--multi-buy-and-the-reference-workshop-layout), which settles the strip as a fixed bottom bar with an always-open panel rather than a sheet that opens and closes.
- **Context:** The dock is five icons — NUMBER, WORKSHOP, LABS, CARDS, MORE — on the one strip of thumb-reachable space a portrait phone has. Three of them cannot be acted on during a run, because permanent purchases are locked while a run is live. Meanwhile Research Focus is chosen once per Prestige and Insight is a single repeatable row, and between them they hold two of the five seats.
- **Decision:** During a run the bar is the four categories (Attack, Defense, Utility, Ultimate) and the dock is hidden; between runs it is RUN, WORKSHOP and MORE. Research Focus, Insight and Prestige move into one Knowledge sheet opened from the Knowledge chip already on screen and from MORE, and the Coins chip opens the Workshop: a currency is the door to its own spend. The run-over screen offers both doors at the moment the currency lands.
- **Consequences:** The four category names are learned once and appear in both lenses. The run screen gets simpler, not busier: Brace moves into the Defense tab and Shield off the screen entirely, so the strip above the bar loses two text actions. The `highest_number` gates at 10 / 1,000 / 110,000 stop gating dock icons and gate rows inside the Knowledge sheet instead, which retires the matching Open item in [`GAME_INVARIANTS.md`](GAME_INVARIANTS.md). `NavDock` gains a second configuration rather than a second class.
- **Revisit when:** A later layer (Breakthroughs, Laws) needs a permanent seat, or playtest shows players cannot find Research Focus at all.

## D017 — Save schema V5 for the four Workshop categories

- **Status:** Accepted (2026-09-21). Implemented (2026-09-21). V5 became a read-only migration source when V6 took over the writer ([D028](#d028--save-schema-v6-and-never-writing-over-a-save-the-loader-cannot-read)).
- **Context:** D013 retires the four bays, moves the Shield Matrix rank into the Workshop as Armor, and retargets Research Focus from a bay to a category. Three saved keys carried the old shape: `workshop.selected_bay`, `focus`, and `tax_resistance_rank` as a field of its own. D007 requires a schema bump and an explicit migration rather than widening V4 silently.
- **Decision:** Save schema V5 keeps every V4 key and meaning except three. `workshop.selected_category` replaces `workshop.selected_bay`; `focus` holds a category id; and `tax_resistance_rank` disappears, because Armor is an ordinary Workshop rank inside `purchased` under the stable id `tax_resistance`. V1 through V4 migrate forward: the Armor rank moves without loss, a retired bay maps to the category that inherited its upgrades, and a live run resumes with identical remaining Liability and RNG state. V2, V3 and V4 become read-only migration sources, so only V5 has a writer.
- **Consequences:** No rank, Coin, Knowledge, Insight or record is lost, and migration rewrites the save in V5 shape immediately. Because Armor is now a Workshop rank it counts toward the Workshop level, so a save holding Armor reaches later rows slightly sooner — never later. Output, Speed and Chance focuses all become Attack and keep every row they had; a Logic focus becomes Utility and keeps one of its three, because Logic's rows split across three categories and no mapping can keep them together. The balance simulator reproduces every documented baseline row unchanged.
- **Revisit when:** Another permanent system adds saved state — the Rig's run-scoped ranks (D015) are the next one, and they belong in the active-run block rather than a new schema of their own.

## D018 — Multi-buy, and the reference Workshop layout

- **Status:** Accepted (2026-09-21) on owner direction, from the reference layout they supplied. Implemented (2026-09-21) for the Workshop; the in-run half waits on the Rig (D015). Holding a card opens its detail too (2026-09-22, owner request), in the Workshop and the Rig.
- **Context:** The owner supplied The Tower's Workshop and in-run screens as the layout they want. Two things in them are not in D013 or D016: the four category buttons are a **fixed strip at the bottom with the panel above them always open**, rather than a sheet the player opens and closes, and each category carries a **buy multiplier** so a ladder can be climbed without one tap per rank.
- **Decision:** The four category buttons are a pinned bottom strip in both lenses — above the nav dock in the Workshop, flush at the bottom in a run, where no dock shows. The panel above them is always open. Cards are compact and two to a row: the stat's name opens a detail panel holding the description and the current and max rank with their values, and the value-and-cost box buys. Each category carries its own `x1 · x5 · x10 · MAX` multiplier. Ranks are priced one at a time and summed, so a press costs exactly what the same ranks cost individually and exactly what it quoted; `MAX` takes every rank the player can afford up to the row's cap. Card values are derived from each row's declared effect rather than authored a second time.
- **Consequences:** A permanently open panel takes the lower part of the run screen, so the ring stage must shrink to about the top half when the Rig lands. That is closer to pillar 1's line than the sheet [`WORKSHOP_DESIGN.md`](WORKSHOP_DESIGN.md) first proposed, and the compact card — no description, detail behind a tap — is what keeps it on the right side. Presets, Respec, the Upgrade/Enhance split and the reference's per-category colours are deliberately not adopted; this HUD carries one accent by an earlier choice. The multiplier is presentation state and is not saved. Multi-buy also makes the Workshop's shortness visible: 51 ranks across 13 rows collapse into roughly thirteen presses, which is the open question this decision opens in the design document.
- **Revisit when:** The rank ladders are deepened, which is what would make `x10` and `MAX` load-bearing rather than convenient.

## D019 — Deep rank ladders: multiply the cap, divide the step

- **Status:** Accepted (2026-09-22) on owner direction. Implemented (2026-09-22).
- **Context:** Multi-buy (D018) made the Workshop's real size visible. Thirteen Coin-funded rows held 51 ranks between them — three to ten each, at cost growth of 1.55 to 2.00 — which `MAX` collapsed into about thirteen presses. D013's context says the owner wants the Workshop to be a large part of the game, as it is in The Tower, whose ladders run to hundreds of levels per stat. A ladder of 51 is climbed once rather than returned to.
- **Decision:** Every ladder is rebuilt to roughly 20× its length with its per-rank effect divided by the same factor, so **the value at maximum rank is exactly what the shallow ladder reached**. Cost growth flattens to 1.038–1.078, and each row's Coins-to-max is designed rather than inherited from its old base and growth. Category gates move from 0 / 2 / 5 / 8 to 0 / 12 / 30 / 60 and Research Focus from 12 to 120, chosen to hold the old *Coin* pacing rather than the old rank counts. Burst is the one row that cannot scale: it sets a tick interval, so it runs six ranks, one tick shorter each, to the same floor of every sixth tick.
- **Consequences:** 51 ranks become 906 for 86,007 Coins against 85,635 — the Workshop buys decisions, not hours, and the whole build-matrix baseline in [`WORKSHOP_DESIGN.md`](WORKSHOP_DESIGN.md) reproduces exactly: the same wave, minutes, hits and Coins on every row, with peak Number under 0.1% adrift on float residue in the per-rank multipliers. The first failed run now funds twelve ranks instead of two, on the same 48 Coins. Two long-standing distortions retire with it: Cushion was 30% of the Workshop's price for a stat that does nothing on Tier 1, and Tap Damage was 0.2% of it for a stat used all game. Small stat values needed decimals on screen, because the Number formatter rounds to whole units and a rank worth 0.05 read as zero. A test asserts every cap still lands on its old value, so a later rank-count change that moves one fails rather than retuning in silence.
- **Revisit when:** The Workshop should take *longer* as well as hold more choices. That is a different dial — the per-row Coin targets — and it would move the balance baseline this decision deliberately preserved.

## D020 — Defense becomes a build: Siphon, Recoil, tier-scaled Cushion, Brace Cost, Second Wind

- **Status:** Accepted (2026-09-22). Implemented (2026-09-22). Siphon and Recoil were given new jobs, Leech and Thorns, by [D038](#d038--siphon-becomes-leech-and-recoil-becomes-thorns) (2026-09-23).
- **Context:** D012 gave Defense a job no Attack stat could do, but left it with one stat that did it. Armor shrank the hit and Cushion did nothing on the tier most players play, so "Defense" was a single number wearing a category's name, and pillar 2 — one build cannot trivially solve both checks — had nothing to stand on.
- **Decision:** Five stats, each working on a different part of being stuck. **Siphon** banks a share of the damage a wave absorbs, so a wave you cannot beat stops being a slow death sentence; it never reduces what the wave takes. **Recoil** deals a share of every hit back to the wave that landed it, turning being hit into progress; a braced boundary deals none, because no hit landed. **Cushion** is priced in the tier's hits rather than in absolute Number, its face value times that tier's pressure multiplier. **Brace Cost** buys the Brace price down from 30% toward a floor of 15%, never to free. **Second Wind** lets one hit that would end a run leave a share of that run's peak Number instead, once per run. Their caps — 25%, 50%, tier-scaled, 15%, 25% — come from the sizes the earlier measurements showed to matter, where Siphon at 10% moved nothing.
- **Consequences:** Defense holds six rows, 460 ranks and 60,008 Coins against Attack's 60,009, which is what makes a Defense Research Focus worth the same as an Attack one: a 25% discount is worth a quarter of what the category costs, so the categories are balanced by Coin cost rather than by row count. Balance target 5 is met — maxed Attack alone reaches Tier 1 wave 90 and maxed Defense alone reaches 25, so wave 100 needs both, and the cheapest route is Attack plus Armor. Armor and Recoil both reach 100 by different routes, 82 hits over 45 minutes against 58 over 39. Two run-scoped fields join the active-run block rather than a new schema, as [D017](#d017--save-schema-v5-for-the-four-workshop-categories) said the next run-scoped system should: the run's own peak Number, distinct from the permanent `highest_number`, and whether Second Wind is spent. A save written before them resumes with an unspent Second Wind. One Must invariant changes with this: Number reaching zero no longer always ends the run.
- **Revisit when:** A Defense stat stops being bought at all, or Recoil's speed advantage over Armor makes Armor the trap rather than the cheap route.

## D021 — Boss Damage, and Utility pays in Coins and Knowledge

- **Status:** Accepted (2026-09-22). Implemented (2026-09-22).
- **Context:** Step 4a made Defense a build, which left two gaps. Bosses end runs and no stat answered them specifically. And Utility held one row, so a Research Focus on it was worth an eighth of a focus on Attack, which is the lopsidedness D019's notes flagged.
- **Decision:** **Boss Damage** multiplies produced damage on boss waves only, to ×2 at its cap. It is read through one `_damage_multiplier()` that taps, ticks and the displayed rate all share, so a boss wave cannot show one number and deal another. **Coin Bonus** lifts everything a beaten wave pays, milestone bonuses included, because a milestone is a wave beaten and one rule reads better than two; it is floored per wave rather than rounded, because "+50% Coins" that sometimes pays +100% reads as a bug. **Knowledge Bonus** lifts what a run ending grants, applied inside `get_prestige_knowledge_gain()` so death and voluntary Prestige cannot disagree. The three categories are then sized so their Coin totals match: Attack 68,008, Defense 60,008, Utility 67,990.
- **Consequences:** The Workshop holds 20 rows, 1,466 ranks and 196,006 Coins. That growth is new content, not repricing: every row that existed before step 4 still costs what it did. Research Focus is a genuine three-way choice, balanced by Coin cost rather than by row count, which is the fix D019's notes asked for and explicitly not a per-category discount scale. Measured, Utility behaves as its category promised — it never wins a wave, reaching the same wave 96 with and without it, and pays 49% more Coins and one more Knowledge for doing so. Balance target 2 needs reading with care now: it measures the D012 retune at equal builds, and Coin Bonus is meant to lift Coins per minute. **Balance target 5 is met but closer than it was:** Boss Damage moved maxed Attack's solo reach from wave 90 to 96, and wave 100 is the line that target defends, so another Attack row of that size would break it.
- **Revisit when:** An Attack row pushes a pure-Attack build to Tier 1 wave 100, which would make Defense optional and empty pillar 2; or Coin Bonus makes farming a comfortable tier strictly better than pushing a hard one.

## D022 — The run-over screen names what the run was lost to

- **Status:** Accepted (2026-09-22) on owner direction. The heading, cause, two gaps and doors implemented (2026-09-22).
- **Context:** [`WORKSHOP_DESIGN.md`](WORKSHOP_DESIGN.md) planned this screen as "What would have saved you": two counterfactual numbers, `2.4× more damage` and `hits 35% smaller`. The owner asked for the opposite framing.
- **Decision:** The screen names the cause, not the cure. It reads `LOST TO`, then the wave and whether a boss landed it, then the size of the hit that emptied the Number. Step 6 added the two gaps underneath (2026-09-22) in the same voice — how far short Attack fell against that wave's HP and Defense against its hit — as facts rather than as prescriptions. The smaller gap carries the accent, pointing at the category to open next without saying so in words.
- **Consequences:** A counterfactual reads as a lecture and quietly tells the player what to buy; a cause tells them what happened and leaves the conclusion to them. It also fits the vision's "every number that kills you was visible before it did" more exactly, because naming the number that did it is a fact about the run rather than advice about the next one. `RunSummary` carries the killing hit, the boss flag and the two gaps, since `_reset_run_state` wipes the encounter and the run Number before the screen can read them; a retreat records none of them, having been lost to nothing.
- **Revisit when:** Playtest shows players cannot tell which category to open next without being told.

## D023 — A Rig rank is worth three Workshop ranks, and the defensive effects get combined ceilings

- **Status:** Accepted (2026-09-22) on owner direction from the target-8 recommendation. Implemented (2026-09-22); the multiplier is the simulator's tuning dial. Refined (2026-09-22): Armor's ceiling now bounds Armor's own combined share (at most 75% off) inside its multiplicative stage, instead of a `cap_min` on the final hit, which would also have clawed back any later rule that shrinks hits for its own reason. Armor alone behaves exactly as before, and the balance baseline is unchanged.
- **Context:** The Rig's first measurement (D015's core) priced one rank at roughly one wave's HP and gave it one Workshop rank's effect. Measured, the Rig was a net loss at every build: fresh 21→21, early 29→23, mid 50→40, attack max 96→94, defense-only 25→22. The Number a rank spends was the buffer absorbing the next 60–100 hits, while the rank's effect added a few percent of damage. Balance target 8 gates the panel, and only two levers move value per Number: more ranks (cheaper) or more effect per rank. Cheaper ranks were measured and rejected — k=0.2 with 1.15 growth turned the game into a runaway (mid reached wave 204, top builds never died and took zero hits). So the effect per rank is the lever.
- **Decision:** One Rig rank grants three Workshop ranks' worth of its effect (`RIG_EFFECT_MULTIPLIER`, per category, mutable so the simulator can sweep it). Because the Rig is uncapped and stacks on top of the Workshop's caps, the combined defensive effects gain ceilings: Armor never reduces a hit below 25% of its base, Siphon never banks more than half the damage dealt, and Recoil never returns more than the hit itself. Armor's ceiling is enforced through the modifier pipeline's `cap_min` stage; Siphon and Recoil clamp where the effect is applied. Burst is exempt from the multiplier: its ranks are tick-interval steps, so it stays one-for-one and keeps its floor of every sixth tick.
- **Consequences:** At the swept value (M=3) target 8 is met — attack max 96→103, attack max + armor 100→109, everything maxed 110→120 — and target 7 still holds (fresh 21→21, one rank affordable). M=2 fails (97 and 107); M=5 and M=8 overshoot (everything maxed 160 and 204+), so 3 is the smallest passing value and the sweep keeps the choice honest. Target 9 holds at every M: purchases stop in the last ten minutes as cost growth outruns income. **Early and mid builds still cannot profit from the Rig** under the simulator's policy — their budget buys too few ranks to beat the buffer they spend — which is an open playtest question, not a target failure. The Workshop's own card values are unchanged; the Rig's panel must show its own per-rank value.
- **Revisit when:** Playtest shows early or mid players cannot profit from the Rig at all, or the Rig needs to headline: the multiplier is one value in `TaxBalanceProfile`, and `run_balance.sh` prints the sweep.

## D024 — Labs: real research, mirroring The Tower, separate from the Knowledge sheet

- **Status:** Accepted (2026-09-22) on owner direction. Implemented (2026-09-22): `LabResearch`, `GameState` integration and the additive save fields; the panel and its entry point on the Workshop screen. Its "always active, run or no run" is refined by [D031](#d031--research-that-finishes-during-a-run-counts-from-the-next-run): a rank finishing mid-run counts from the next run. Its two fixed slots are superseded by [D029](#d029--lab-slots-start-at-one-and-open-to-five-with-gems): one to start, up to five, opened with Gems.
- **Context:** D016's Knowledge sheet absorbed "Labs, Insight and Prestige" as one sheet, but the code it absorbed was never The Tower's Labs: `labs_content` and `cards_content` in `main.gd` are the surviving variable names of the original five-icon dock (NUMBER, WORKSHOP, LABS, CARDS, MORE), repurposed to hold Research Focus and Insight respectively once those turned out to be the only mechanics behind those two icons. The owner's correction: Cards and Labs were never actually built, so folding placeholder names into one sheet wasn't the consolidation it looked like, and Labs specifically should work the way it does in The Tower — permanent research, paid in Coins, gated by real time — not as a rename of Insight. Cards do not need a parallel new system: the Rig (D015) already is a run-scoped, tactical spend from the same categories, which is what Cards are for in The Tower.
- **Decision:** `LabResearch` (`src/lab_research.gd`) is the new domain owner, alongside `TaxBalanceProfile` and `TaxEncounter` rather than inside `GameState` (architectural law 7). Four lines to start, one per Workshop category with rows today plus a Main line, each its own Coin cost ladder and real-time duration ladder: **Lab Speed** (Main, shortens every other line's duration, floor 60%), **Damage Research** (Attack, `base_output_multiplier`), **Resilience Research** (Defense, `collection_resistance`), **Coin Research** (Utility, `coin_bonus`). Ultimate is skipped, matching `has_category_content` gating: it has no rows to research yet either. Two lines can run at once (`LAB_SLOTS`), so prioritising is a real choice rather than a queue. Starting and collecting are between-run actions like the Workshop (Coins are already a between-run resource), but a line's timer runs in real time regardless of run state or whether the app is open, using an absolute `started_unix` settled lazily whenever it's read — the same explicit-time seam `apply_offline` already uses for testability, via an optional `now_unix` parameter rather than a hidden wall-clock read. A finished rank folds into `_effect_sum` / `_effect_product` exactly like a Workshop rank, so it is permanent and always active, run or no run. `lab_ranks` and `lab_active` are additive save fields on V5, following the Rig's own precedent (D015) for a new permanent system that doesn't change any existing key's shape.
- **Consequences:** Labs is a second permanent progression axis alongside the Workshop, both spending Coins, with genuinely different shapes: Workshop is bought on the spot with Coins in hand, Labs is queued and waits on a clock you can't skip. The Knowledge sheet (Research Focus, Insight, Prestige) is unchanged; nothing here reopens D016. The confusingly-named `labs_content` / `cards_content` containers in `main.gd` are left alone, since renaming them is an unrelated cleanup, not part of this fix. Costs and durations are starting proposals, sized by hand like the Rig's `RIG_COST_K` was before its sweep — `run_balance.sh` doesn't cover them yet.
- **Revisit when:** Ultimate gains rows and can open its own Lab line; playtest shows two concurrent slots are too few or too many; or the Workshop/Labs Coin split needs its own balance pass the way D019 gave the Workshop one.

## D025 — The run screen becomes a landing page between runs, not an idle loop

- **Status:** Accepted (2026-09-22) on owner direction. Implemented (2026-09-22): the landing panel, and the START RUN button restyled as a filled pill.
- **Context:** The owner was weighing an "infinite loop" — dying or retreating dropping the player straight back onto the same tap-ready screen — against a deliberate landing page to prep on before the next run. The run screen already stopped hard at death (the "Lost to" modal, D022) and already required an explicit RUN button press to start again, so nothing here was truly infinite; but the screen underneath the modal reused the exact tap-stage shell with its labels swapped to "NOT RUNNING" / "START A RUN TO PRODUCE", showing an empty ring and a zeroed Number with nothing live to say. The vision's loop table treats the meta step between runs as one that's supposed to matter — "the meta loop exists to make the next run different, not to skip the run loop" — which argues for a real landing beat, not a race back into tapping.
- **Decision:** Between runs, the stage's rect (where the ring and the giant Number sit during a run) shows a landing panel instead: `READY`, then the last run's tier and wave, then what ended it (`RETREATED`, `PRESTIGED` or `LOST TO A HIT OF …`) and what it paid in Coins and Knowledge, or `NO RUNS YET` before a first attempt. The ring, the Number and the tap hint hide with it. This is a state branch on the existing "number" screen, not a new tab or a separate screen, keeping pillar 1's one-screen simplicity intact. The RUN button itself changes with it: retreating stays the quietest control on the screen (flat text, D012's original reasoning still holds — it's destructive and rare), but starting is now a filled ACCENT pill, the same treatment the run-over screen's CONTINUE door already uses, so the one thing there is to do standing between runs looks like the thing to do.
- **Consequences:** No new save state, no new screen, no change to what a run does — this only changes what's shown while one isn't running. Tier switching (the tier button above the stage) and the Armor shortcut are unaffected and still work from the landing state. A future pass could add a live preview of the next run's starting stats from the permanent Workshop; the owner deferred that to keep this landing page to run history only.
- **Implementation note (2026-09-22):** A successful Prestige now replaces the last run summary and the landing says `PRESTIGED`, with that run's Coins, Gems and Knowledge. It previously showed the preceding run or `NO RUNS YET` because Prestige did not make a summary.
- **Revisit when:** Playtest wants a starting-stats preview on the landing panel, or the filled RUN button reads as loud enough that Retreat needs to move further from it.

## D026 — What The Tower's home screen contributes to the landing panel, and what it doesn't

- **Status:** Accepted (2026-09-22) on owner direction, from a screenshot of The Tower's home screen. Implemented (2026-09-22) as placeholders: the difficulty line and the category rank row.
- **Context:** The owner supplied The Tower's home screen (currency bar, tournament and event countdowns, MISSIONS, a settings/leaderboard/mail icon column, MILESTONES, Total Coin Bonus, a Difficulty card with tier/highest wave/reward, a Dissonant boosts row of four category multipliers, BATTLE, and a Dissonant Run variant) and asked to scan it and add what D025's landing panel needs, placeholder for now. Foundation-completeness law and the anti-goals list both apply: a reference screen having an element is not evidence Number Go Up needs it, and live-ops scaffolding is explicitly out before the core run is proven fun.
- **Decision:** Two things map onto data Number Go Up already has and earn a placeholder now: a **Difficulty line** — the selected tier, its best wave and its reward multiplier, plus the total Coin Bonus multiplier (Workshop's Coin Bonus row and Labs' Coin Research combined) — and a **category rank row**, four tiles (Attack, Defense, Utility, Ultimate) showing total Workshop-plus-Lab ranks per category as a plain count rather than a fabricated single multiplier, since Attack alone already spans several different effects with no one number that represents it honestly. Both read existing `GameState` data (`get_tier_best`, two new small getters `get_category_rank_total` and `get_coin_bonus_multiplier`) and add no new mechanic. **Not brought over:** the tournament trophy, the event calendar, the castle and ticket countdowns, and MISSIONS are all live-ops scaffolding the anti-goals list rules out before the core run is proven fun; the settings/leaderboard/sell/mail icon column has no Number Go Up equivalent to surface (no leaderboard, no social, no store) and duplicates what the drawer and MORE already do for what does exist; MILESTONES is already covered by the tier record and the run-over screen's gaps, not a separate screen; the Dissonant Run hard-mode variant is a whole system (Dissonance) with no foundation gap evidence behind it, and Gems would be a fourth currency pillar 4 says the game doesn't need yet.
- **Consequences:** The landing panel now carries three blocks: last run, difficulty, and category ranks. All are read-only placeholders — no new save state, no new purchase path. The category row deliberately shows a rank count instead of a headline multiplier like the Tower's ×7680.29, because Number Go Up's damage math doesn't collapse into one number per category the way a single "Coin Bonus" stack does; the Coin Bonus line is the one place a single multiplier is honest today.
- **Revisit when:** The category row's plain rank count stops being informative enough and a real per-category power score is designed; or Ultimate gains rows and its tile stops always reading zero.

## D027 — Cards: a real, Gem-pulled collection, toned down from the reference

- **Status:** Accepted (2026-09-22) on owner direction, from a screenshot of The Tower's Cards screen. Implemented (2026-09-22): `CardCollection`, `GameState` integration, the sheet and its entry points. Its placeholder Gem trickle is replaced by [D030](#d030--gems-from-boss-waves-and-per-tier-milestone-checkpoints)'s two sources. Reaffirmed (2026-09-22) after the architecture audit flagged that random pulls, rarities and duplicate levelling conflict with a deterministic-acquisition brief: the owner keeps Cards, under that name and as built, for now, and may revisit them later (for example as deterministic Protocols).
- **Context:** The Cards screen showed something D024 hadn't accounted for: Cards are a permanently-owned collection pulled with Gems (not bought outright like the Rig), levelled by repeat pulls, equipped from a capped Active set distinct from the full Inventory, swappable between named loadout presets, and — once a card maxes — tied into a Mastery research line in Labs. That's a real fourth structure, not the Rig wearing a different name. The owner's call: bring the gacha pull in, toned down from the reference's aggressiveness, and design the Gem economy itself (pull cost, pity, sources) later. This directly reopens D026's exclusion of Gems as an unearned fourth currency — the owner's direction here is the later, controlling instruction.
- **Decision:** `CardCollection` (`src/card_collection.gd`) is the new domain owner, alongside `LabResearch`. Six cards to start — Damage, Attack Speed, Coins, Critical Chance (common), Health, Extra Defense (rare) — each a flat per-level step over `MAX_LEVEL` (7), reusing existing effect keys rather than inventing new mechanics. Toned down from the reference in three specific ways: one flat step per level rather than the reference's separate stars-and-levels split; **no named loadout presets** — one Active set, changed by hand, since presets need a reason to exist before they earn the UI (the same reasoning D018 gave Presets and Respec); and **no Mastery-into-Labs tie yet** — `LabResearch`'s catalogue is a fixed list today, and a card dynamically opening a research line is a real architecture change that needs its own pass once a maxed card exists to design against. A pull costs `PULL_COST_GEMS` (20, matching the reference's shown price) and draws one card weighted 70/30 common/rare, uniform within a tier; a new card is owned at level 1, a repeat pull levels it by one to the cap. Only an **Active** card (capped at `ACTIVE_SLOTS`, 4 to start) contributes to `_effect_sum` / `_effect_product`; an owned card sitting in Inventory does not, which is the one place Cards genuinely differ from Labs and the Workshop, where every owned rank always counts. Pulling, equipping and unequipping are between-run actions, like the Workshop and Labs. `gems`, `card_ranks` and `card_active` are additive V5 save fields, following the same precedent Labs set. Gems have no real source yet — a placeholder trickle of 1 Gem per milestone wave keeps a pull reachable for testing and play until the owner's economy pass replaces it.
- **Consequences:** A third permanent progression layer now exists alongside the Workshop and Labs, with a genuinely different rule: ownership and equipping are separate, where the other two are "owned = counts." `RARITY_WEIGHT`, `PULL_COST_GEMS`, `ACTIVE_SLOTS` and every card's per-level step are starting proposals sized by hand, the same way the Rig's `RIG_COST_K` and Labs' costs and durations were before their own tuning passes — none of this is balanced against the rest of the economy yet.
- **Revisit when:** The owner designs the real Gem economy (sources, pity, pull cost) and this placeholder trickle needs replacing; a maxed card is common enough that Mastery research is worth building into Labs; or multiple loadouts (Presets) earn a reason to exist the way D018 asked for.

## D028 — Save schema V6, and never writing over a save the loader cannot read

- **Status:** Accepted (2026-09-22) on owner direction ("yes to V6"), from the architecture audit's save findings. Implemented (2026-09-22).
- **Context:** The audit found three ways to lose every piece of permanent progress with no message. A save the loader did not recognise — written by a newer build, torn by a crash mid-write, or holding a wrong-typed block — loaded as a fresh game, and the next autosave, 20 seconds later, wrote that fresh game over it; a wrong-typed block stopped the load halfway and left a partial state to be written instead. Saves were written straight over the live file, so an interrupted write tore it. And the Rig, Labs, Cards and Gems had all been added to V5 after it shipped, so the version number no longer said which fields a save held: an older build would accept a newer V5 save and silently write it back without them. Separately, a resumed run drifted from the run that was saved, because the tick phase and the crit chain were not saved (D006).
- **Decision:** Save schema V6 declares every field added to V5 after it shipped and adds `tick_accumulator` and `critical_chain` to the active-run block; V5 becomes a read-only migration source. From here on a new saved field bumps the version rather than widening the current one. Nothing the loader cannot read is ever written over: a save from a newer build is left untouched and saving pauses (reported on load and in the drawer); an unreadable live save is moved aside intact under `.unreadable-<unix time>` and the backup loads instead, or a fresh game starts and says so. The loader checks each block's type before assigning any of it, so a load either happens whole or not at all. A save is written to a temp file and swapped in only once complete, and the save it replaces becomes the `.bak` a later load falls back to. A migration keeps the file it read as `.v<N>-backup.json` before writing the migrated save. Clearing the save removes all of these. `ScientificNumber` reads a non-finite value as zero rather than looping forever.
- **Consequences:** The worst an interrupted write or a torn file can cost is the last autosave interval. Running an older build against a newer save loses that session's progress rather than the save. The version number again says exactly which fields a save holds, so every future system pays for its saved state with a migration, which is the price of the guard. Swapping the file in is a rename, which has not yet been checked on the Web export, where `user://` is synced to browser storage.
- **Revisit when:** The Web export shows the rename is not persisted reliably; or saves move to a cloud or account, which needs conflict resolution on top of this.

## D029 — Lab slots start at one and open to five with Gems

- **Status:** Accepted (2026-09-22) on owner direction ("Labs start with 1 slot, can have up to 5. Use gems to unlock the others, similar to tower"). Implemented (2026-09-22) with save schema V7. The Gem shortfall its consequences describe is answered by [D030](#d030--gems-from-boss-waves-and-per-tier-milestone-checkpoints).
- **Context:** D024 gave Labs two fixed slots. The owner wants the reference's shape instead: a slot count that grows, bought with the premium currency, so how many lines research at once becomes a long-term goal rather than a given.
- **Decision:** A new game has one Lab slot, and slots open one at a time with Gems up to five. The second, third, fourth and fifth cost 20, 40, 80 and 160 Gems — starting proposals sized against a Card pull, authored here rather than taken from the reference (D009), until the Gem economy pass. Opening a slot is a between-run action, like every other Gem and Coin spend. The slot count is permanent and saved as `lab_slots`, which under D028 bumps the save schema to V7. Every save from before V7 keeps two slots, because every player had two, so no one loses one; a stored count is clamped to between one and five.
- **Consequences:** Existing players keep two slots and can buy three more; new players start with one. **Gems are now scarcer than these prices assume.** Their only source is the placeholder trickle of one per milestone (D027), and since the milestone reload fix a milestone pays once per tier record: four per tier, twelve in all across Tiers 1–3. That is below the 20 Gems either a second slot or a Card pull costs. Before the fix, every reload re-paid the milestones, which is what made pulls reachable in play. The Gem economy pass (D027) has to add a repeatable source before either spend is reachable for a new player.
- **Revisit when:** The Gem economy pass sets real sources and prices; or playtest shows one slot makes Labs feel inert early.

## D030 — Gems from boss waves and per-tier milestone checkpoints

- **Status:** Accepted (2026-09-22) on owner direction ("build both gem sources. Go big on milestones, per tier similar to the tower"), after researching how The Tower pays Gems. Implemented (2026-09-22) with save schema V8.
- **Context:** Once milestones paid only once (the reload fix), D027's placeholder of one Gem per milestone came to twelve Gems in all, below the 20 a Card pull or a second Lab slot (D029) costs. The Tower's Gems come from one-time tier-and-wave milestones (the largest source, paid per tier and growing with it), small in-run pickups, and ads, timers, missions, tournaments, events, guilds and purchases. Only the first two fit a local, deterministic game with no store and no live operations (D011, the vision's anti-goals); the rest need ads, a server or a wall clock a player can move.
- **Decision:** Two sources, both fixed rules rather than random rolls. **Every boss wave beaten pays one Gem, every run** — the in-run pickup's shape, earned by play and capped by how far the run goes. **Every tier has thirteen milestone checkpoints**, waves 10, 20, 25, 30, 40, 50, 60, 75, 90, 100, 125, 150 and 200, each paying Gems once per tier record: twice the square root of the wave, times the tier's reward multiplier, rounded. That makes Tier 1's checkpoints worth 208 Gems (6 at wave 10 to 28 at wave 200), Tier 2's 377 and Tier 3's 545. The formula and the checkpoint list are ours, not the reference's table (D009). The four Coin checkpoints at 10, 25, 50 and 100 keep their Coin bonus exactly, so the Coin economy and the balance baseline do not move. A checkpoint a record has already passed but not claimed pays on load, exactly what claiming it would, so a checkpoint added later never makes a player re-clear a wave to be paid; and a save from before the bump has the checkpoints it claimed at one Gem topped up to the new table, once. The run's Gems appear on the run-over screen, the landing panel and a toast. Save schema V8 adds the run's Gems and, by its version, marks the top-up as done.
- **Consequences:** Measured with the simulator: a first run to wave 21 pays 17 Gems, so a first Card or second Lab slot comes after the second run; repeat Tier 1 runs pay 2 to 11 Gems; a first push into Tier 2 pays 29 to 68. Every checkpoint on all three tiers totals 1,130 Gems (725 up to wave 100) against 300 for every Lab slot, so Labs fill over the early game and Card pulls become the long Gem sink. The owner's own save gains 14 Gems on its first load. Prices (D027, D029) are unchanged and remain placeholders; the Gem economy pass can retune both sides with one constant each.
- **Revisit when:** The Gem economy pass sets real prices; playtest shows Gems arriving too fast for Labs to feel earned, or too slow once the checkpoints are spent; or a tier gains waves past 200 that need checkpoints.

## D031 — Research that finishes during a run counts from the next run

- **Status:** Accepted (2026-09-22) on owner direction ("next run for labs"), from the architecture audit's finding F6. Implemented (2026-09-22).
- **Context:** D024 made a finished Lab rank "always active, run or no run". Because a rank settled whenever an effect was read, and settling read the wall clock, a line finishing mid-run changed that run's damage: two runs with the same seed and inputs produced different output depending on when research happened to end, the balance simulator could not reproduce a run with Labs in it, and a frozen run resumed after time away came back stronger.
- **Decision:** A line keeps researching in real time, run or no run, app open or closed. A rank settles only between runs: when a run starts, when any run ends (death, retreat or Prestige), and when a save taken between runs loads or the Labs sheet is read between runs. Effects read settled ranks only, so evaluating one never reads the clock. During a run the Labs sheet shows a finished line as `DONE · NEXT RUN`. Remaining time is clamped to the line's duration, so a clock set back cannot show more time than a line takes.
- **Consequences:** A run's power never changes with the wall clock, so D006's seed replay holds with Labs in play, and permanent power moves only between runs, as the Workshop's already does. A line that finishes early in a long run waits for the run's end, and its slot stays busy until then; starting research is already a between-run action, so no choice is lost. No save change: a mid-run save simply resumes with the finished line still waiting.
- **Revisit when:** Playtest shows players expect research to land mid-run, or a Lab line is designed specifically to act inside a run.

## D032 — The run screen keeps the Number on top and the Rig below it

- **Status:** Accepted (2026-09-22) on owner direction ("do the run screen layout"), from the architecture audit's finding F4. Implemented (2026-09-22).
- **Context:** The Rig panel shipped as an overlay across the whole run screen, from the wave line down to the category strip. During every run it covered the ring and the Number at every portrait size, the Brace, Armor and Retreat controls collided with the category strip, and the RUN · WORKSHOP · MORE dock stayed on screen although D016 hides it during a run. D018 had already said the ring stage must shrink to about the top half once an always-open Rig panel landed; that half of the work was never done.
- **Decision:** During a run the screen is split top to bottom. The stage — ring, Number, rate and the tap hint, which moves under the Number — starts under the wave line and takes half the free height, never less than 170 pixels on the 540-wide canvas; the hit line and Brace sit under it; the Rig panel, always open as D018 requires, fills the rest down to its category strip, flush at the foot of the screen. The dock hides on the run screen, as D016 says, but shows on any other tab mid-run, so the Workshop always has a way back. Retreat moves to the top right: still quiet text, now out of the thumb's reach, which suits an action that is destructive and rare (D025). The Armor shortcut hides during a run, because Armor is a Workshop rank and cannot be bought mid-run. Between runs nothing changes. Floating numbers rise from the Number wherever the stage sits.
- **Consequences:** The Number is visible and central throughout a run at every captured size, from 320×568 to a tablet; on the smallest phone the Rig panel shows one full row of cards and scrolls. A tap on the stage produces, and a tap on the Rig panel does not. Brace stays on the run screen rather than moving into the Defense tab as D015 and D016 planned: it is timing-critical, and behind a tab it would cost a tap exactly when the hit is coming. That move remains open.
- **Revisit when:** Brace moves into the Defense tab; a run-screen system such as Ultimates needs room; or playtest shows the Rig panel is too cramped on small phones, where a collapsible panel is the next step.

## D033 — Tier 1 opens under attack

- **Status:** Its warm-up is replaced by [D040](#d040--one-difficulty-curve-for-tier-1-from-wave-1)'s single curve (2026-09-23). Its rule that a timed-out warm-up wave pays as if beaten is superseded by [D037](#d037--the-number-always-rises-missed-waves-move-on-bosses-stay-and-fight) (2026-09-23). Accepted (2026-09-22) on owner direction, after a playtest: "Takes a long time to tap your way to 200 odd score before selecting an upgrade", "the grace period is a little too generous", "the player should feel 'oh, right I'm being attacked'", "without a heavy penalty". Implemented (2026-09-22); D034 retunes its opening prices and hits.
- **Context:** Measured from a fresh save, the first five minutes had nothing in them. Tier 1's twenty grace waves carried no HP and no hit, a new game had no automatic output at all until a Workshop rank was bought (which only happens between runs, about six minutes in), and the Rig's first rank cost 238 Number, one to two minutes of tapping. The first hit a new player ever took, at wave 21, was the one that ended the run. Tiers 2 and 3 are pressured from wave 1, so Tier 1 taught nothing about the game its later tiers play. A straight ramp on the full curves was measured and rejected: early waves stayed trivial and the first real hit came from the wave-10 boss and killed.
- **Decision:** Tier 1's twenty warm-up waves are small but real. Wave HP starts at 30 and grows 12% a wave, the hit starts at 1 and grows 12% a wave, and a warm-up boss multiplies both by 1.4 rather than tripling HP. A warm-up wave that outlasts its timer lands its hit and then ends, paying its Coin, record and checkpoint as if beaten, so the warm-up keeps its five-minute pace instead of stalling on a wave a new player cannot yet beat; past the warm-up a stuck wave keeps its HP as D012 says. A Tier 1 run starts with 50 Number, so the first hit costs Number rather than the run. Every run produces a flat 1 a second from its first second, which upgrades do not raise, so a new game is never idle at zero without making a built-up one stronger. A run's first two Rig purchases during the warm-up are quoted at a tenth of the first full wave's HP (24 Number), whatever the rows; every later purchase pays the D015 price. All seven values live in `TaxBalanceProfile`, and `run_balance.sh` prints an OPENING table at 0, 2, 3, 4 and 6 taps a second (`-- --opening` prints only that).
- **Consequences:** Measured at seed 7: the first Rig purchase is affordable from the first second; an idle player is hit within 15 seconds and dies around wave 13 at three and a quarter minutes; at 2, 3 and 4 taps a second the first hit lands at 105 to 150 seconds (the wave-10 boss for a steady tapper) and the run ends at wave 21 after about five and a quarter minutes with 48 Coins, as balance target 1 requires; at 6 taps a second it reaches wave 22 with 62. The first-run representative is unchanged at wave 21 and 48 Coins. A steady tapper who keeps pace is still not hit inside the first minute: HP growing faster than that would collide with wave 21 and end first runs early. Beyond the opening, measured across four seeds, maxed Attack alone is unchanged (96 to 98, so balance target 5 holds), while the top Rig builds reach about five waves deeper (everything maxed with the Rig, 120 to 130 against 117 to 123) and mid builds now profit slightly from the Rig (40 to 45 against 40), because the flat base and the two cheap ranks give the Rig's reinvest policy a little more to work with; Rig purchases still stop well before the end of long runs (target 9). The fresh-build matrix row that reinvests aggressively now dies at wave 17, since the warm-up's hits punish spending the buffer; the opening table's buffer-keeping player reaches 21.
- **Revisit when:** Playtest shows steady tappers need a hit inside the first minute; the Rig's top-end reach needs pulling back (the multiplier sweep in `run_balance.sh` is the dial); or later tiers want a warm-up of their own.

## D034 — Make the Tier 1 opening kinder

- **Status:** Its warm-up is replaced by [D040](#d040--one-difficulty-curve-for-tier-1-from-wave-1)'s single curve (2026-09-23). Its discounted warm-up Rig prices are superseded by [D039](#d039--rig-prices-follow-the-players-income-not-the-wave) (2026-09-23). Accepted (2026-09-22) on owner direction after another Tier 1 playtest: "Quite hard for an intro to a game." Implemented (2026-09-22).
- **Context:** D033 made the first two Rig ranks affordable but priced each at about 24 of a starting 50 Number. Buying both left about 2 Number. A fresh player buying two Attack ranks and tapping once per second died at wave 8; an idle player who bought both died at wave 3. The first full hit jumped from about 12 at warm-up wave 20 to 156 at wave 21. A trial flat reduction of 2 for 8 Number instead let an idle player bank all 48 warm-up Coins, so it did not suit this opening.
- **Decision:** The first two Rig purchases during Tier 1's warm-up cost 5% of the first full wave's HP, about 12 Number each. Warm-up Hit grows 8% per wave instead of 12%; its Wave HP, twenty-wave length, starting Number and Coin rewards stay as D033 set them. For Tier 1 alone, the effective Hit at waves 21–25 is limited to twice the final warm-up boss Hit for each next wave, until the full curve catches up at wave 26. This reduction enters as a multiplicative Collection modifier, so Armor still counts. The base encounter curve and Tier 2/3 values are unchanged. No flat-defence row enters either progression layer from this balance pass.
- **Consequences:** In the seed-7 fresh-run sweep with two immediate Attack Rig buys, zero, one, two and three taps per second end at about waves 15, 18, 22 and 23, earning 38, 41, 62 and 76 Coins. The first hit still lands after 15 seconds when idle. The cautious two-tap representative still ends at wave 21 with 48 Coins. Max Attack alone remains below wave 100 while Attack plus Armor reaches it, and Tier 2 results are unchanged. This is simulator evidence; a phone playtest remains the check on feel. The faster 6-tap opening now reaches wave 25 and pays 107 Coins, so early Coin pacing deserves watching.
- **Revisit when:** A phone run is still too punishing, the extra minutes before a pressured run ends feel slow, or high-tap early rewards erode the value of the first Workshop purchases.

## D035 — Lower Workshop prices for the first permanent build

- **Status:** Accepted (2026-09-22) on owner direction after another Tier 1 playtest: "still brutal. Workshop needs balanced a little more. Lower the cost of upgrades. Adjust their pricing scaling accordingly." Implemented (2026-09-22).
- **Context:** The first run through the warm-up pays 48 Coins including one-time checkpoint bonuses. Under D019's deep ladders, those Coins could buy 12 Tap Damage ranks with one Coin left; each rank adds only 0.05 damage per tap. The owner supplied The Tower's Workshop curve families as a shape reference: cheap early ranks, smoothly rising capped stats, and warning-worthy cliffs on select later rows. Its Coin amounts and content do not map to this game's rewards or stat values.
- **Decision:** Keep the current 20 Workshop rows, caps, unlock levels and per-rank effects. Halve each row's starting Coin cost. Adjust each row's geometric growth so the final rank costs about three quarters of its old price before whole-Coin rounding. This makes the first ranks substantially cheaper while keeping the capped ladders rising smoothly; no new cliff, unlock charge, currency or flat-defense stat is added. Only permanent Workshop Coin prices change. Rig Number prices, Lab costs, Coin rewards and saved rank keys stay as they are.
- **Consequences:** The full Workshop costs 134,163 Coins rather than 196,006 at list price: Attack 46,590, Defense 41,070, Utility 46,503. Opening Tap Damage, Damage per Second and Armor cost 2, 2 and 4 Coins, down from 3, 4 and 8. The first 48 Coins can buy 12 Tap Damage, six Damage per Second and one Armor rank, leaving one Coin. At seed 7 and two taps per second, that mixed build reaches wave 24 and earns 71 repeatable Coins, versus wave 23 and 56 for the old affordable 12-Tap build. The first run itself still has no Workshop ranks and is unchanged. Already-owned ranks and saves retain their value; future ranks are cheaper. This is simulator evidence, not a phone playtest.
- **Revisit when:** Repeated-run playtesting shows the first build still feels weak, the cheaper ladders erase meaningful Coin choices later, or a distinct long ladder is designed for a core stat rather than simply importing The Tower's thousands of levels.

## D036 — Let the first Tier 1 run bank Number

- **Status:** Its warm-up is replaced by [D040](#d040--one-difficulty-curve-for-tier-1-from-wave-1)'s single curve (2026-09-23). The two discounted Rig opening prices it kept were retired by [D039](#d039--rig-prices-follow-the-players-income-not-the-wave) (2026-09-23). Accepted (2026-09-22) on owner feedback after D035: "still feels brutal." Implemented (2026-09-22).
- **Context:** D035 lowered permanent Workshop prices, but the first run starts with no Workshop ranks and cannot buy one until it ends. At one tap per second with the two opening Rig buys, the first wave's 30 HP consumed almost all output and the player banked little Number before Hits drained it. The simulator ended that run at wave 18 with 41 Coins; an idle run ended at wave 15 with 38. The earlier Workshop change therefore could not fix the felt first-run problem.
- **Decision:** Tier 1 warm-up Wave HP starts at 20 rather than 30 and grows 8% per wave rather than 12%. The first six ordinary waves are now within a one-tap-per-second player's roughly 30 damage per wave, allowing output to overflow into Number. Warm-up Hit growth moves only from 8% to 8.5%: its twenty Hits must add up to more than the 50 starting Number, so doing nothing cannot collect the whole warm-up reward. Keep the 50 starting Number, the two Rig opening prices, the 20-wave warm-up, Coin rewards and the pressured-wave base curves. This is balance profile v4. No flat-defense stat or new mechanic enters the first run.
- **Consequences:** At seed 7 with the cautious opening Rig policy, a zero-tap run ends before the warm-up completes (wave 16, 39 Coins), one tap per second reaches wave 21 with 48 Coins, and two taps reaches wave 23 with 76 Coins. Before this change those outcomes were waves 15/38, 18/41 and 22/62. A true no-action run with no Rig purchases ends at wave 20 with 43 Coins; that separate case closes an idle-reward gap the earlier policy missed. The representative two-tap first run now lasts 495 seconds and ends at wave 23 with 76 Coins, rather than 360 seconds at wave 21 with 48. Max Attack remains below wave 100, Attack plus Armor reaches it, and Tier 2 results are unchanged in the deterministic simulator. A saved active encounter retains its stored remaining HP; later warm-up waves use the new profile. This is simulator and test evidence, not a phone playtest.
- **Revisit when:** The longer first run feels slow, active players go too long without seeing a Hit, or a fresh phone run still feels punishing despite banking Number from the first waves.

## D037 — The Number always rises; missed waves move on; bosses stay and fight

- **Status:** Accepted (2026-09-23) on owner direction: "I don't think the number should stop going up until you clear the specific amount within a 15 second window", "the game needs to be simple, easy to understand but keep the underlying model of, make number go up, defend against, clear [each wave's amount], fight boss waves", and "yes, bosses stay, build step 1". Supersedes [D012](#d012--output-beats-the-wave-before-it-becomes-number), and D033's rule that a timed-out warm-up wave pays as if beaten. Implemented (2026-09-23) with balance profile `tax-foundation-v5`; no curve, reward or price changed.
- **Context:** The owner found the game constrained and unforgiving. Under D012 the Number stood still while a wave had HP: at one tap a second a fresh run had flat stretches of up to 105 seconds in its first three minutes. A stuck wave hit every 15 seconds until beaten, so runs ended in a slow drain. Beaten waves still waited out their timer, which was 37–41% of every run past the first.
- **Decision:** (1) Everything produced is Number, and the same output also counts against the active wave; lifetime production counts it once. (2) A wave sets an amount to clear within 15 seconds. (3) A beaten wave gives way to the next once it has been on screen for `MIN_WAVE_SECONDS` (2.5), and pays as before. (4) An ordinary wave that outlasts its timer lands its Hit once and moves on, paying its Coins times the share of its HP cleared, floored like Coin Bonus. It was not beaten, so it sets no record and pays no Gem; its checkpoint, if it has one, pays once a later wave is beaten. (5) A boss (every tenth wave, warm-up bosses included) stays until beaten and hits at every boundary, as stuck waves did before. (6) A Hit that takes the Number to zero still ends the run, Second Wind aside. The warm-up's curves, starting Number, opening Rig prices and the Tier 1 transition Hits are unchanged.
- **Consequences:** Seed 7, two taps a second unless noted. The Number first visibly rises within a second, and the longest flat stretch in any fresh run is 1.5 seconds. The representative first run reaches wave 29 in 6.9 minutes with 56 Coins, against wave 23, 8.3 minutes and 76. Missed warm-up waves no longer pay their Coin, which is where most of the drop comes from. One tap a second reaches wave 26 with 48 Coins; doing nothing reaches wave 26 in 9 minutes with 30 Coins, so the D036 contract that an idle run ends inside the warm-up is restated as "doing nothing earns clearly less than tapping". Runs are much shorter at every build (mid: wave 50 in 10.3 minutes rather than 19.0; max Attack: wave 105 in 20.2 rather than 97 in 39.3), and Coins per minute rise about 1.5–1.8×. **Balance target 5 now fails:** max Attack alone reaches wave 105, because Attack's output is also the Number that absorbs Hits. Adding Armor still helps (wave 120), and Defense decides boss fights and missed waves, but the Hit sizes need a retune, with Guard ([`WORKSHOP_EXPANSION.md`](WORKSHOP_EXPANSION.md)), before wave 100 needs Defense again. Save shape is unchanged; a saved run resumes under the new rules at its stored wave and HP. Side effects: `highest_number` now rises with all output, so the Workshop and Knowledge unlocks keyed to it (1,000 and 110,000) open sooner; Second Wind restores a share of a larger peak; and a checkpoint on an ordinary wave that was missed pays once a later wave is beaten (the same rule the D030 load catch-up applies), so a run and a reload always agree. Balance target 2 (Coins per minute) also fails by design until Coins are retuned or the target is restated.
- **Revisit when:** A phone playtest finds runs too short or bosses too long, the Hit retune cannot restore target 5 without making the opening harsh again, or partial Coins make missing waves a better farm than clearing them.

## D038 — Siphon becomes Leech and Recoil becomes Thorns

- **Status:** Accepted (2026-09-23) with D037, on owner direction not to cut Workshop content. Implemented (2026-09-23).
- **Context:** Under D037 all output is already Number, so Siphon (a share of damage dealt reaches Number) did nothing, and Recoil (a share of a Hit is dealt back to the wave) hit a wave that had just moved on.
- **Decision:** The `siphon` row is now **Leech**: while a boss stands, its share of the damage the boss takes is added to the Number a second time. The `recoil` row is now **Thorns**: its share of every Hit is dealt to the wave in front of you, which is the boss that landed it or the wave that replaces a missed ordinary wave. A braced boundary still deals none. Row ids, rank ladders, prices, caps, Rig rows and the D023 combined ceilings (50% and 100%) are unchanged, so every owned rank keeps counting.
- **Consequences:** Both rows become boss-fight stats, which is what [`WORKSHOP_EXPANSION.md`](WORKSHOP_EXPANSION.md) designs around. Measured on max Attack: Leech moves wave 105 to 110 and Thorns to 108 (faster, 18.9 minutes rather than 20.2). A player who bought Siphon for its old job sees it change; that is this decision, not a silent retune.
- **Revisit when:** Either row stops being bought, or bosses stop being the fights D037 made them.

## D039 — Rig prices follow the player's income, not the wave

- **Status:** Accepted (2026-09-23) on owner report: "After a few purchases on any item the costs massively shoot up from like 10 to 436 or something like that. Surely that can't be intended … I can't play the game in its current state effectively." Supersedes D015's rule that Rig prices are quoted against the wave's HP, and the D033/D034 warm-up discount. Implemented (2026-09-23).
- **Context:** A Rig rank cost `k` × the current wave's HP (floored at Tier 1 wave 21's 238) × 1.5–1.7 per rank already owned. The first two warm-up buys were discounted to about 12 Number, so the third jumped to about 380–690 Number while a new player made 2–3 a second. Prices also rose with every harder wave whether the player bought anything or not.
- **Decision:** A rank costs `k` × `RIG_PRICE_SECONDS` (5) seconds of the player's steady income × 1.4 per rank of that row already owned. Steady income is the passive rate plus one tap a second, without the boss-only bonus. `k` stays 1 for Attack and Defense and 2 for Utility. The warm-up discount is removed: a fresh run's first rank costs about 10 Number anyway. A bulk press prices each rank at the income it will have by then, so it costs exactly what buying singly would. Rig effects, rows and ceilings are unchanged.
- **Consequences:** At a fresh start, six ranks of a row that doesn't raise income cost about 10, 14, 20, 27, 38 and 54 Number; a row that does, such as Damage per Second, climbs a little faster (10, 16, 24, 37, 56), because each rank also raises the income the next price is quoted against. Harder waves no longer raise prices by themselves. Seed 7, two taps a second:
  - One tap a second with the cautious opening policy reaches wave 25 in 5.5 minutes with 58 Coins (was wave 26 and 48 under D037 with wave-priced ranks).
  - The simulator's reinvest policy now buys 45 ranks in a fresh run, 43 at mid and 121 at max Attack. Mid holds wave 50 with 11% more Coins in 22% less time.
  - The Rig pays at the top: max Attack goes from 105 to 125, and everything maxed from 130 to 164.
  - Target 7 holds (a fresh Rig run reaches wave 25, not the Workshop build's 30). Target 9 holds (every run ends; purchases thin to 1–11 in the last ten minutes).
  - A 1.3–1.6 growth sweep is in `run_balance.sh`: 1.3 let everything maxed reach 197; 1.5 and 1.6 barely helped max Attack (108 and 107).
  - From the independent review, against the parent commit: deeper top runs pay more. Max Attack with the Rig earns 43% more Coins, and max Attack plus Armor with the Rig earns 61 Gems rather than 12, because it now passes the checkpoints beyond wave 100. Tier 2 gains are modest and every run still dies (max Attack 33 → 36, plus Armor 40 → 50, plus all Defense 50 → 57).
  - **Less headroom before endless runs:** at a 5× Rig effect multiplier (swept, not shipped), everything maxed is still alive at 90 minutes (wave 302), where wave pricing died at 160. The shipped 3× ends every run, but a future Rig-effect increase such as the percentage Boosts must re-sweep this.
  - Rig ranks that add crits, Double Tick, Burst or Boss Damage raise real output without raising prices, and so do taps beyond one a second, so crit-heavy builds and heavy tappers pay fewer real seconds per rank. This is a consequence of "steady income", and the simulator shows no runaway from it.
  - A bulk quote applies only the quoted row's own effect per rank rather than recomputing income, so the Rig panel's eight-times-a-second re-quote costs what it did before (about 1–4 ms for a full Attack tab at MAX).
- **Revisit when:** The percentage Boosts proposed in [`TIER_BALANCE_PROPOSAL.md`](TIER_BALANCE_PROPOSAL.md) replace the Workshop-rank-sized Rig effects, which will need the growth re-swept, or playtests find the Rig too strong against Workshop investment.

## D040 — One difficulty curve for Tier 1 from wave 1

- **Status:** Accepted (2026-09-23) on owner report and direction: "The difficulty hits a huge spike at wave 21. I think a remnant of the old system. Rework this and let's decide on math that will decide the difficulty curve of tier 1", then "yes" to the proposed maths with Hits at 60% of HP. Supersedes the D033/D034/D036 warm-up (its HP and Hit formulas, softer bosses, 1-Coin waves and the wave 21–25 Hit ramp) and D010's warm-up Coin payouts. Implemented (2026-09-23) as balance profile `tax-foundation-v7`.
- **Context:** Waves 1–20 used a separate warm-up formula (HP +8% a wave from 20, Hits from 1), and wave 21 switched to the full curve: HP rose from 80 at wave 19 to 238 at wave 21, Hits doubled every wave from 7 at wave 20 to 208 at wave 25, and Coins jumped from 1 to 14 a wave. A single constant-growth curve was tested and rejected: it let a mid Workshop build reach wave 80 instead of 50, because Workshop damage grows ever more slowly with Coins spent and a curve rising like wave² matches that.
- **Decision:** One set of rules from wave 1, shared by every tier through D002's multipliers.
  1. **Wave HP** = 4 × (0.05 × wave^2.13 + 0.8 × wave + 1.5), ×1.08 every 10 waves, ×1.2 every 50 and ×1.5 every 100: the curve waves 21+ already used, now starting at wave 1 (9 HP).
  2. **Hit** = a share of that wave's HP: 20% at wave 1, rising evenly to 60% by wave 30, then 60%. The owner approved 60%; the ramp was added in the build after the simulator showed a player who spends every Number on Rig ranks died to the wave 10 boss's first Hit at 1½–2 minutes at every tap rate.
  3. **Bosses** (every 10th wave): HP ×3 and Hit ×1.5 of an ordinary Hit at that wave; they stay until beaten (D037).
  4. **Coins** = 0.65 × wave × the tier's reward multiplier, ×5 on a boss, from wave 1.
  5. Tier 1 still starts a run with 50 Number (now a tier field, not a warm-up), and every run still produces 1 a second before any Workshop rank.
- **Consequences:** Seed 7:
  - **First run:** the representative two-tap run reaches wave 20 in 5.6 minutes with 106 Coins (was wave 29, 6.9 minutes, 56 Coins). A player who buys a Rig rank whenever affordable reaches wave 16 at one tap a second (104 Coins) and wave 20 at two (146). Doing nothing reaches wave 10 with 9 Coins.
  - **Builds:** early 30, mid 50, max Attack dies at the wave 100 boss, max Attack plus Armor 110, plus all Defense 120.
  - **Targets:** balance target 5 holds again for Workshop builds (wave 100 needs Defense). Tier 2 all-Defense is unchanged at wave 50.
  - **Hit share:** from wave 30 a Hit is 60% of its HP everywhere, a little more than the old separate Hit curve at depth (the wave 100 boss hits 16,643 rather than 12,251).
  - **Remaining step:** the ×1.5 milestone at wave 100, on top of the boss's ×3, doubles that boss in one step; it is the tier gate and is left as is.
  - **Saves:** a run saved under an older profile is rebuilt on the current curve when it loads, keeping the share of its wave already cleared. The retired wave 21–25 Hit ramp was applied at read time and never stored, so a save at those waves would otherwise have landed the full old Hit (156 at wave 21, against the 13 the player was shown). Saves under the current profile resume exactly.
  - **Tiers 2 and 3 change too**, because they multiply this curve. Their early Hits are much lighter (Tier 2's wave 1 Hit 38 rather than 250; wave 21 2,267 rather than 3,121), and their deep Hits heavier (the Tier 2 wave 100 boss 332,852 rather than 245,016). The 20×/60× ratios hold at equal waves; all-Defense Tier 2 still reaches wave 50, and Cushion is still worth about two waves there.
  - **Target 5's qualifier:** Workshop-only max Attack dies at the wave 100 boss, but with in-run Rig spending on Attack rows alone it reaches wave 106. Wave 100 needs Defense or the Rig.
  - **Watch the bosses:** boss HP is now 3.5–3.7× the wave before at waves 10, 20 and 30 (the warm-up bosses were 1.4×), and every early build in the simulator ends on a boss (idle at 10, the first run at 20, early at 30, mid at 50). That is "fight boss waves" by design, but it is the first thing to check in a playtest.
- **Revisit when:** A phone playtest finds the first boss or the first run too short, or the wave 100 step reads as a spike rather than a gate.
